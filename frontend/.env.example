# Base URL of the backend API (see ../backend)
# During local dev this is usually http://localhost:4000
# In production, point this at your deployed backend, e.g. https://api.cloudorbit.fun
VITE_API_BASE_URL=http://localhost:4000
